const prompt = require('prompt-sync')();

let number = Number(prompt("Enter number : "));

for ( count=1; count<=$number ; count++ )
        if (count == number )
        then
                printf 1/count
        else
            if(number == number)
                printf 1/count
            else
                printf 1/count"+"